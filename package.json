{
  "name": "filmila",
  "version": "1.0.0",
  "description": "Filmila video streaming platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd frontend && npm run build",
    "dev": "cd backend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
